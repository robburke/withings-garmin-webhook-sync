# Local-daemon dependencies (post-Lambda cutover, 2026-04-13).
# All Garmin Connect calls now go through garmin-connect-mcp via subprocess,
# so the heavyweight garth/garminconnect/pydantic stack is gone.

requests>=2.31.0
python-dotenv==1.0.0
colorlog==6.8.0
